dependencies:
  override:
    - bundle install
    - gem install s3_website
  cache_directories:
     - "~/.rvm"
test:
  override:
    - "exit 0"
deployment:
  production:
    branch: master
    commands:
      - bundle exec jekyll
      - s3_website push
