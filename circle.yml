dependencies:
  override:
    - bundle install
    - gem install jekyll s3_website
test:
  override:
    - "exit 0"
deployment:
  production:
    branch: master
    commands:
      - bundle exec jekyll
      - s3_website push
