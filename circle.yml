dependencies:
  override:
    - bundle install
test:
  override:
    - bundle exec jekyll
    - bundle exec s3_website push --dry-run
deployment:
  production:
    branch: master
    commands:
      - bundle exec jekyll
      - bundle exec s3_website push
