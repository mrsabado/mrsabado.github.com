dependencies:
  override:
    - bundle install
test:
  override:
    - "exit 0"
deployment:
  production:
    branch: master
    commands:
      - bundle exec jekyll
      - bundle exec s3_website push
