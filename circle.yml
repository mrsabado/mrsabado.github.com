dependencies:
  override:
    - gem install jekyll s3_website
  cache_directories:
     - "~/.rvm"
test:
  override:
    - "exit 0"
deployment:
  production:
    branch: master
    commands:
      - jekyll
      - s3_website push
